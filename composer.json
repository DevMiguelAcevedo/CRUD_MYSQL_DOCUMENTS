{
    "name": "crud/documentos",
    "description": "CRUD de Gestión de Documentos - PHP MVC",
    "version": "1.0.0",
    "license": "MIT",
    "authors": [
        {
            "name": "Tu Nombre",
            "email": "tu.email@example.com"
        }
    ],
    "require": {
        "php": "^7.0"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "Config\\": "config/"
        }
    },
    "scripts": {
        "start": "cd public && php -S localhost:8000",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
